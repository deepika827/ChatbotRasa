import type { DriverOptions, UserConfig } from "../common/config.js";
import { Server } from "../server.js";
import type { LoggerBase } from "../common/logger.js";
import { DeviceId } from "../helpers/deviceId.js";
export declare abstract class TransportRunnerBase {
    protected readonly userConfig: UserConfig;
    private readonly driverOptions;
    logger: LoggerBase;
    deviceId: DeviceId;
    protected constructor(userConfig: UserConfig, driverOptions: DriverOptions, additionalLoggers: LoggerBase[]);
    protected setupServer(): Server;
    abstract start(): Promise<void>;
    abstract closeTransport(): Promise<void>;
    close(): Promise<void>;
}
//# sourceMappingURL=base.d.ts.map